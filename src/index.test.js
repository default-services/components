import {
  Button,
  Checkbox,
  Container,
  Input,
  Navbar,
  RadioButton
} from '.';

describe('Button', () => {
  it('is truthy', () => expect(Button).toBeTruthy());
});

describe('Checkbox', () => {
  it('is truthy', () => expect(Checkbox).toBeTruthy());
});

describe('Container', () => {
  it('is truthy', () => expect(Container).toBeTruthy());
});

describe('Input', () => {
  it('is truthy', () => expect(Input).toBeTruthy());
});

describe('Navbar', () => {
  it('is truthy', () => expect(Navbar).toBeTruthy());
});

describe('RadioButton', () => {
  it('is truthy', () => expect(RadioButton).toBeTruthy());
});